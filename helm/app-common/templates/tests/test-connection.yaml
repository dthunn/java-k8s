apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-common.fullname" . }}-test-connection"
  labels:
    {{- include "app-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
