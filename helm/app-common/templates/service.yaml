apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-common.fullname" . }}
  labels:
    {{- include "app-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-common.selectorLabels" . | nindent 4 }}
